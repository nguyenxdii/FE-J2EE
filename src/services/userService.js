const BASE_URL = 'http://127.0.0.1:8080/api';

export const userService = {
  uploadCCCD: async (formData) => { /* Chờ code ngày mai */ },
  getBalance: async (userId) => { /* Chờ code ngày mai */ },
  adminGetAllUsers: async () => { /* Chờ code ngày mai */ },
  adminApproveKYC: async (kycId, status) => { /* Chờ code ngày mai */ },
  // ... các hàm khác cho đủ 6 chức năng
};